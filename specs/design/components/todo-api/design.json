{
  "name": "todo-api",
  "type": "service",
  "version": "0.1.0",
  "language": "Ballerina",
  "buildpack": "docker",
  "appPath": "todo-api",
  "entrypoint": "deployment/service",
  "exposure": "intranet",
  "dependencies": [
    { "kind": "platform-resource", "name": "todo-db", "resourceType": "postgres-cnpg" },
    { "kind": "platform-resource", "name": "user-auth", "resourceType": "thunder-app" }
  ],
  "description": "Owns the todo entity: create, list, and toggle done/not-done for the signed-in caller's own todos, persisted in a dedicated Postgres database. It does not manage user identity — that is asserted by the gateway from the signed-in caller's token — and it exposes no editing or deletion of todo text.",
  "stories": [2, 3, 4, 5],
  "skillsPinned": ["openapi-conventions", "ballerina", "api-management", "thunder-authentication"]
}
